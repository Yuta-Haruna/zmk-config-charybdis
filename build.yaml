include:
  - board: nice_nano
    shield: charybdis_left      # ← 反対側）
  - board: nice_nano
    snippet: zmk-usb-logging       # ← Studio用RPCをUSB/UARTで有効化
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n  # ← Studio機能ON
    shield: charybdis_right   # ← 中央（ホスト/BLE名が出る側）
